

## Step1:
- clone the mongodb repo from https://github.com/Arqui-software-grupo03/mongodb.git
- `cd mongodb`
- `docker-compose up`

## Step 2
- Open a new terminal
- go to the posts repo
- `cd posts`
- `docker-compose build` && `docker-compose up`
- try it:
        http://localhost:8000/admin
